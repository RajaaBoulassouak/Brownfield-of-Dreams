<section class="dashboard-main">
  <h2> <%= current_user.first_name %>'s Dashboard </h2>

  <%= button_to 'Log Out', logout_path, method: 'delete', class: "btn btn-primary mb1 bg-teal" %>
  <h3>Account Details</h3>
  <ul>
    <li> <%= current_user.first_name  %> <%= current_user.last_name %> </li>
    <li> <%= current_user.email%> </li>
  </ul>
  <section>
    <h1>Bookmarked Segments</h1>
  </section>
  
  <% unless current_user.token.nil? %>
    <section>
      <h1>Github Repositories</h1>
        <% @facade.repos.each do |repo| %>
          <ul class="repo">
            <li class="name"><%= link_to repo.name, repo.link %></li>
          </ul>
        <% end %>
    </section>

    <section>
      <h1>Followers</h1>
        <% @facade.followers.each do |follower| %>
          <ul class="followers">
            <li class="follower-name"><%= link_to follower.name, follower.url %> </li>
          </ul>
        <% end %>
    </section>

    <section>
      <h1>Following</h1>
        <% @facade.following.each do |user| %>
          <ul class="following">
            <li class="following-name"><%= link_to user.name, user.url %> </li>
          </ul>
        <% end %>
    </section>
  <% end%>
</section>
